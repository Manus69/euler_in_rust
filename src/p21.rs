
use crate::euler;

fn compute_sums(N: usize) -> Vec<u64>
{
    let mut sums: Vec<u64> = vec![0; N];

    for k in 0..N
    {

    }

    return sums;
}

pub fn p21()
{
    println!("{:?}", euler::divisors(284));
}