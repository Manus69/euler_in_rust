#![allow(dead_code)]

mod euler;
mod p24;

fn main()  
{
    p24::p24();
    // let mut v = vec![4, 3, 1, 2];
    // euler::permute(&mut v);
    // println!("{:?}", v);
    // euler::permute(&mut v);
    // println!("{:?}", v);
}